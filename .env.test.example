# Target database is created automatically if missing (connects to `postgres` first). Requires CREATEDB or superuser.
ELEPHANTMQ_TEST_PG_URL=postgres://postgres:postgres@localhost:55432/elephantmq_test
# Optional: max clients for the shared test pg.Pool (default 32).
# ELEPHANTMQ_TEST_PG_POOL_MAX=32
